package fr.adlmrl.mtrfra.neoforge;

//? if neoforge {

/*import fr.adlmrl.mtrfra.mod.Init;
import fr.adlmrl.mtrfra.mod.registry.MTRFRARegistry;
import fr.adlmrl.mtrfra.mod.util.Constants;
import net.neoforged.api.distmarker.Dist;
import net.neoforged.bus.api.IEventBus;
import net.neoforged.fml.common.Mod;
import net.neoforged.fml.loading.FMLEnvironment;

@Mod(Constants.MOD_ID)
public class MainNeoForge {

    public MainNeoForge(IEventBus eventBus) {
        Init.init();
        MTRFRARegistry.attachEventBus(eventBus);

        if (FMLEnvironment.dist == Dist.CLIENT) {
            Init.initClient();
        }
    }

}
*///? }
